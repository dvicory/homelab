apiVersion: gateway.envoyproxy.io/v1alpha1
kind: ClientTrafficPolicy
metadata:
  name: trusted-edges
  namespace: gateway
spec:
  clientIPDetection:
    directSourceIP: {}
  requestID: Generate
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: household
