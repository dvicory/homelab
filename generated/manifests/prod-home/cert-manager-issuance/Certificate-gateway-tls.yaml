apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gateway-tls
  namespace: gateway
spec:
  dnsNames:
    - '*.plus2.danielvicory.dev'
    - '*.backup.plus2.danielvicory.dev'
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: gateway-tls
