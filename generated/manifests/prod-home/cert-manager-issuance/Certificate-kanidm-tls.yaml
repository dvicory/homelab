apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kanidm-tls
  namespace: identity
spec:
  dnsNames:
    - idm.plus2.danielvicory.dev
    - idm.backup.plus2.danielvicory.dev
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: kanidm-tls
