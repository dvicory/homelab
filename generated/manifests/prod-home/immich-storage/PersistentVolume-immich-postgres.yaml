apiVersion: v1
kind: PersistentVolume
metadata:
  annotations:
    argocd.argoproj.io/sync-options: Prune=false,Delete=false
  name: immich-postgres
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 32Gi
  claimRef:
    name: immich-postgres
    namespace: immich
  local:
    path: /srv/state/immich-postgres
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - compute-1
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  volumeMode: Filesystem
