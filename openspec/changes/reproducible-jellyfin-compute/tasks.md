## 1. Review boundary

- [x] 1.1 Review the K3s/Flannel, fixed user-namespace mapping, Nix manifest delivery, retained-data, and operator-only SSH access choices and reconcile the artifacts. Evidence: the operator explicitly approved proceeding after review, then approved host bind mounts, workload-level storage failure handling, explicit update recovery, and native facilities before custom machinery. The proposal, delta spec, design, and tasks reflect those decisions; implementation and deployment are not claimed complete.

## 2. Host and guest configuration

- [ ] 2.1 Add the Den compute entity and NixOS-owned Incus envelope using existing aspect, settings, persistence, and access-resolution conventions. Supply native preseed with Nix attrsets; instance operations only verify its resources. Declare isolated non-root host ID translation, resource limits, private networking, retained directories, and node-essential startup prerequisites without changing physical storage or legacy Hermes. Application-only storage must not gate node boot. Verify exact evaluated option leaves, host-specific inclusion, safe expanded settings, and a conflict check before first preseed adoption.
- [ ] 2.2 Add the standalone secret-free guest image and K3s configuration against the unchanged pinned nixpkgs: current user-namespace runtime settings, native snapshotter, bundled networking, and disabled unused ingress/dynamic storage components. Verify the generated kubelet/kube-proxy/containerd configuration parses under the pinned tools, the guest derivation evaluates without decryption, and the built image contains no runtime private key. Record Linux-builder limitations rather than claiming an unbuilt image passed.
- [ ] 2.3 Integrate host-managed runtime guest identity using existing secretRequests/agenix-rekey conventions, atomic staging, translated ownership, read-only attachment, and separate pinned host/guest SSH trust. Support re-staging and explicit identity rotation under the lifecycle lock. Verify matching identity starts the identity-dependent path and missing or mismatched private/public identity blocks it without substituting a generated key. Use disposable test keys locally; production key creation/rekeying requires later authorization.

## 3. Persistent workload and managed resources

- [ ] 3.1 Generate Jellyfin namespace, explicitly bound retained PVs/PVCs, Recreate deployment, service, probes, non-root security context, resource bounds, read-only media, persistent config, and disposable cache. Pin and preload the actual workload image and required K3s images through Nix. Prove native bind-export and mount-propagation behavior with the selected Kubernetes volume type: absent media blocks only its consumers, loss never exposes substitute storage, and restored storage reaches the workload without restarting the node. Verify generated resources against the pinned API/schema and run the actual image to prove startup, config writes as the declared identity, and denied media writes; leave runtime evidence open if it cannot be exercised.
- [ ] 3.2 Use stable Nix-delivered manifest names and native K3s AddOn pruning for objects omitted from updated manifests. Separate retained storage and namespace from disposable workload resources; introduce no custom inventory or pruning controller. Verify removal of a disposable object, survival of an unrelated object, retained-data preservation/reattachment, and safe retirement before deleting an entire manifest file.
- [ ] 3.3 Provide operator-private access and bridge-aware enforcement for workload and Kubernetes management ports without public listeners, physical-uplink bridge changes, or broad trusted interfaces. Cover same-bridge guests as well as routed traffic; do not assume NAT or FORWARD rules alone suffice. Verify local-only tunnel binding, separate SSH trust for both hops, and generated rules locally; prove authorized and unauthorized reachability at runtime before declaring private exposure complete.

## 4. Lifecycle and recovery tooling

- [ ] 4.1 Provide thin inspection and safe instance-creation operations over existing CLIs, accepting pinned artifacts without a separate orchestration database. Use host-side serialization, structured absence handling, effective-envelope verification, node-essential storage/identity preflight, and bounded readiness reporting. Wire workstation build/copy and independent guest deployment through existing deployment conventions and explicit SSH jump configuration. Verify reruns preserve a conformant instance; daemon errors, conflicts, unsafe drift, and identity mismatches cannot trigger creation/deletion or privilege expansion. Missing application media must be reported as a workload failure without stopping the node.
- [ ] 4.2 Separate acknowledged same-version replacement, OS/Kubernetes updates with an unchanged Jellyfin pin, and operator-triggered Jellyfin upgrades. Stage replacement artifacts before deleting only the instance/root. For application upgrades, retain old software, prevent reconciliation from restarting the quiesced workload, confirm no config writer remains, preserve a complete consistent recovery copy with matching software/revision, and verify capacity and copy completion before starting the new version. Use standard Kubernetes/copy/snapshot operations before custom wrappers. Document explicit paired data/software restoration and loss of post-copy application changes. Verify lifecycle serialization, no deletion after failed preflight, interrupted-copy safety, refusal when a consistent recovery point cannot be obtained, and retained-data recoverability after failure; do not imply a Nix downgrade reverses database migration.

## 5. Local integration and operator evidence

- [ ] 5.1 Run focused local Nix evaluation/build checks for the host, guest, images, and lifecycle packages; run formatter/linter once after integration. Verify unrelated fleet outputs still evaluate without changing the lockfile. Report exact commands and distinguish evaluated derivations from built outputs.
- [ ] 5.2 Supply and execute one isolated Linux integration scenario exercising unprivileged Incus, nested K3s, real Jellyfin state, instance/root/cluster deletion, and reconstruction. Include node boot without media, runtime media loss and restoration without node restart, continued management/unrelated workload availability, denied substitute-directory access and media writes, missing/mismatched identity, unsafe drift, native manifest retirement, and safe application-upgrade recovery. If no suitable authorized runtime is available, leave this task unchecked and report the missing runtime rather than substituting mocked success.
- [ ] 5.3 Deliver the operator runbook and observed-evidence record: actual build/deploy/inspection/access interfaces, durable inputs, one-time Jellyfin setup, same-version replacement, OS/Kubernetes updates, explicit application upgrade and paired restore, artifact origins, capacity requirements, and permission/mount checks. Prefer direct standard commands to new wrappers. Verify instructions match implemented interfaces and distinguish local results, guest-loss recovery, and unproven host-loss/backup recovery.

## 6. Target verification — blocked until separately authorized

No command in this section is authorized by the current request. Do not mark it complete from local evaluation or a different runtime.

- [ ] 6.1 Obtain permission for read-only inspection of hvn-hyp1, then check actual encryption/mounts, free space, kernel/cgroups, Incus resources, subordinate-ID collisions, route collisions, media readability, and independent management access. Record the observed prerequisites; stop before any required storage-permission or isolation expansion rather than inventing a compatibility workaround.
- [ ] 6.2 Obtain permission for secret provisioning and host/guest deployment; check conflicts before first preseed adoption, create only approved slice-owned resources, deploy, privately complete new-instance Jellyfin setup, and verify real playback plus denied media writes. Check effective unprivileged maps and actual network restrictions, including same-bridge peers and management ports. Prove node boot without application media, media loss/return without node restart, and continued unrelated workload/management access. Keep incompatible-kernel, filesystem, or nesting results as failed gates; no privileged fallback.
- [ ] 6.3 Obtain explicit guest-deletion approval and execute the design's destruction acceptance twice: retain declared inputs, remove root and cluster database, recreate through repository tooling, then verify the same user, library, recorded playback state, and representative playback without setup. Record evidence of fresh cluster state and preserved application state; validate failed-prerequisite behavior and confirm no retained data or unrelated host service was changed. Only this evidence closes the target guest-rebuild acceptance gate.
