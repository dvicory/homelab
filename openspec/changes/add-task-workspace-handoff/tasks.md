> Prerequisite: do not implement until `add-sandbox-workspace-service` has passed QA acceptance, been verified, and been archived. Revalidate this change after that archive.

## 1. Broker Foundation

- [x] 1.1 Extract the repeated SQLite connection, migration, and transaction setup from workspace, environment-registry, and access-grant services into one shared broker database service without behavior changes; run all focused registry/grant/workspace/environment tests and `.#checks.aarch64-darwin.gondolin-broker-effect`, then commit independently.
- [x] 1.2 Add task-run activation persistence, control activation/consumption, and validation on every ensure/execution/file route; prove completed/older runs cannot recreate or borrow a newer generation, run focused tests and the broker Nix check, then commit independently. QA handoff integration remains unwired.
- [x] 1.3 Add compact revision and import migrations plus strict domain schemas; cover state transitions, uniqueness, bound-field conflicts, and clean QA migration with focused tests and the broker Nix check, then commit independently. QA handoff integration remains unwired.
- [x] 1.4 Use pinned `rsync` for selected-root staging and child materialization; implement bounded detached-tree manifest validation, opaque revision IDs, broker-versioned digests, fsync/verification, and restart reconciliation; cover unsafe nodes, every broker limit, a fixed digest vector, copier/database failures, concurrent replay, tampering, recovery, and independent-copy semantics, run the broker Nix check, then commit independently. QA handoff integration remains unwired.
- [x] 1.5 Add gated, policy-authorized, control-only idempotent publication and one-source private-import operations; atomically bind activation consumption/publication and destination workspace reservation/import, reverify before import, keep execution and disabled routes absent, and test response replay, conflicts, policy denial, tampering, and source isolation before committing independently. QA handoff integration remains unwired.

## 2. Hermes Vertical Slice

- [x] 2.1 Add additive Kanban persistence for the `inherit_parent_workspace_output` task-creation intent and non-empty normalized `workspace_outputs` completion roots, plus a control-socket client for activation, publication, and import; cover fresh/migrated storage, invalid roots, opaque response handling, and unchanged ordinary defaults before committing independently.
- [x] 2.2 Add a generic required-hook API that propagates failures, a durable idempotent publication/preparation operation journal, and required pre-completion publication through the workspace plugin; preserve best-effort lifecycle observers, prove failed publication leaves the task running, pending/ready operations replay without duplicate work, conflicting preparation requests fail closed, and ordinary completion remains unchanged, then run the Hermes Nix check and commit independently. Dispatcher-side import consumption remains part of 2.4.
- [x] 2.3 Extend existing Kanban create/completion schemas with exactly `inherit_parent_workspace_output` and `workspace_outputs`; bind inherited input to the caller's trusted current task and direct child link, reject caller-selected sources, storage IDs, host paths, remapping, and multiple inputs, run focused schema/dispatcher tests and the Hermes Nix check, then commit independently. No host activation.
- [x] 2.4 Extend the repository workspace-service plugin and Gondolin backend to register trusted Kanban run activations, attach non-model task/run identity to every broker call, publish/fence on completion, import before child spawn, and fail closed without fallback; cover ordinary no-output completion, retry, stale runs, broker outage, crash replay, metadata nondisclosure, normal conversation behavior, and non-Gondolin regressions, run focused tests and the Hermes Nix check, then commit independently. Activates only behind the QA handoff gate.

## 3. QA Integration and Acceptance

- [x] 3.1 Wire broker-owned manifest limits, policy actions, service hardening, and the explicit handoff gate through the secure-terminal module and QA runner profile; keep revision storage derived beneath the broker state directory, evaluate `hvn-hyp1`, build focused broker/Hermes checks, and document contained QA reset/rollback before committing independently.
- [ ] 3.2 After explicit approval, deploy only `hvn-hyp1` QA and verify selected-file publication, storage-metadata nondisclosure, ordinary no-output completion, one inherited child import, private child mutation, same-task retry, stale-run rejection, normal conversation behavior, unsafe-file and size-limit failures, broker restart before/after both database commits, duplicate/conflicting IDs, forged/unrelated/cross-board input, tampered content, fail-closed outage, bounded storage, and unchanged production/Podman behavior; record broker-derived evidence and commit the acceptance record independently.
