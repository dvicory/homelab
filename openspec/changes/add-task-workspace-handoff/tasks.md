> Prerequisite: do not implement until `add-sandbox-workspace-service` has passed QA acceptance, been verified, and been archived. Revalidate this change after that archive.

## 1. Broker Foundation

- [x] 1.1 Extract the repeated SQLite connection, migration, and transaction setup from workspace, environment-registry, and access-grant services into one shared broker database service without behavior changes; run all focused registry/grant/workspace/environment tests and `.#checks.aarch64-darwin.gondolin-broker-effect`, then commit independently. No SOPS update.
- [ ] 1.2 Add task/run attempt activation and epoch persistence, control registration/consumption, and validation on every ensure/execution/file route; prove completed/older runs cannot recreate or borrow a newer generation, run focused tests and the broker Nix check, then commit independently. QA feature remains off; no SOPS update.
- [ ] 1.3 Add revision, entry, operation, and import migrations plus strict domain schemas; cover state transitions, uniqueness, request-digest conflicts, and clean QA migration with focused tests and the broker Nix check, then commit independently. QA feature remains off; no SOPS update.
- [ ] 1.4 Implement bounded canonical traversal, copy-based staging, opaque revision IDs, versioned manifest digests, fsync/verification, and restart reconciliation; cover unsafe files, identity changes, every limit, fixed digest vectors, and injected filesystem/database failures, run the broker Nix check, then commit independently. QA feature remains off; no SOPS update.
- [ ] 1.5 Add control-only idempotent publication and one-source private-import operations; bind trusted source/destination authority and policy, reverify before import, prove execution routes cannot manage revisions, and test response loss, conflicts, tampering, and source isolation before committing independently. QA feature remains off; no SOPS update.

## 2. Hermes Vertical Slice

- [ ] 2.1 Extend generic Kanban persistence with `finalizing`, immutable finalization/preparation records, selected output roots, one logical source task, and broker provenance; migrate existing boards without changing default behavior, run focused upstream tests and `.#checks.aarch64-darwin.hermes-worker-lane`, then commit independently. No host activation or SOPS update.
- [ ] 2.2 Add a required completion-finalizer interface and dispatcher replay for stale finalization/preparation records; preserve existing best-effort observers for unrelated lifecycle events, prove failures cannot mark tasks done or duplicate work, run focused tests and the Hermes Nix check, then commit independently. No host activation or SOPS update.
- [ ] 2.3 Extend existing Kanban create/completion schemas with exactly `inherit_parent_workspace_output` and `workspace_outputs`; bind inherited input to the caller's trusted current task and direct child link, reject caller-selected sources, storage IDs, host paths, remapping, and multiple inputs, run focused schema/dispatcher tests and the Hermes Nix check, then commit independently. No host activation or SOPS update.
- [ ] 2.4 Extend the repository workspace-service plugin and Gondolin backend to register trusted Kanban run activations, attach non-model task/run identity to every broker call, publish/fence on completion, import before child spawn, and fail closed without fallback; cover ordinary no-output completion, retry, stale runs, broker outage, crash replay, metadata nondisclosure, normal conversation behavior, and non-Gondolin regressions, run focused tests and the Hermes Nix check, then commit independently. Activates only behind the QA feature; no SOPS update.

## 3. QA Integration and Acceptance

- [ ] 3.1 Wire the revision root, canonicalization version, limits, policy actions, service hardening, and explicit handoff gate through the secure-terminal module and QA runner profile; evaluate `hvn-hyp1`, build focused broker/Hermes checks, and document contained QA reset/rollback before committing independently. No SOPS update.
- [ ] 3.2 After explicit approval, deploy only `hvn-hyp1` QA and verify selected-file publication, storage-metadata nondisclosure, ordinary no-output completion, one inherited child import, private child mutation, same-task retry, stale-run rejection, normal conversation behavior, unsafe-file and size-limit failures, broker restart before/after both database commits, duplicate/conflicting IDs, forged/unrelated/cross-board input, tampered content, fail-closed outage, bounded storage, and unchanged production/Podman behavior; record broker-derived evidence and commit the acceptance record independently. No SOPS update.
