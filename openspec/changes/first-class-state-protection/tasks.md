## 1. Authority and reuse seam

- [x] 1.1 Add a focused evaluation fixture against the pinned gen-schema and gen-scope APIs; verify the smallest viable identity-bearing records, typed references, `mkClaim`/`resolveClaims` results, unresolved reporting, provenance, and strict forcing before defining the full model.
- [x] 1.2 Add ADR-0009 recording that Den/gen owns state identity, policy/target resolution, coverage, and capability wiring while mature backup/replication tools retain lifecycle ownership; verify its MADR metadata links this change and relates to ADR-0006 and ADR-0008 without modifying existing ADRs or active changes.
- [x] 1.3 Record the inspected owner capabilities and limits for pinned zrepl 0.7.0, Restic 0.19.1 with the NixOS module, resticprofile 0.33.1, borgmatic 2.1.7, and SelfHostBlocks `991a496b`; verify the implementation plan delegates scheduling, retention, pruning, incremental/resumable transfer, repository management, filesystem snapshots, and database dumps to the selected owner and does not import SelfHostBlocks.

## 2. Den/gen state-protection model

- [x] 2.1 Declare the Den `preserve` pipe and gen-schema registries for reusable/referenced StateSlot, State, ProtectionPolicy, Route, Target, and Integration values; keep Realization, nested access projections, non-authoritative `physicalBacking`, shared Integration+Target native wiring, optional Binding, and ScratchDestination typed without independent registry identity unless implementation proves it necessary. Verify Nix uses typed object refs, generated manifests use stable IDs, and host/aspect-local declaration sugar contributes the same global typed State + Realization model.
- [x] 2.2 Implement stable State identity and policy precedence; verify declaration module, host, Realization kind/path, application version, package, selected Integration, and policy changes leave `stateId` and gen identity unchanged, production/QA remain distinct, equal-precedence policy conflicts fail, and moving one declaration/Realization between hosts preserves identity while simultaneous authoritative Realizations fail.
- [x] 2.3 Implement the State-to-Route gen-scope cascade with capability-first Integration selection, actual configured consistency/fidelity guarantees, logical Target resolution, reusable Integration+Target native wiring, optional minimal Binding overrides, and source/Target coverage validation. Verify missing/duplicate Realization, invalid ref, missing Target/native wiring, uncovered child, unsupported source capability/operation/semantic guarantee, contradictory or duplicate Binding/wiring, zero/multiple Integration fulfillment, and resource collision report State/Route context. Verify `filesystem-read` without an owner-managed stable view guarantees only `live` and cannot satisfy a stronger StateSlot requirement, Route identity is independent of Target/Integration identity, a normal compatible State needs no Binding, and one ambiguous Route remains one obligation until Route or Binding selects an Integration.
- [x] 2.4 Compile schema-versioned `desired-inventory` and `executable-plan` documents from the same declarations; verify plan-only gaps serialize without backend access, plan-only States cannot operate, typed Nix refs become stable manifest IDs/resolved values, logical Target remains separate from shared Integration+Target native destination wiring, semantic payload representation remains distinct from native retained-point representation, optional Binding absence is explicit, `physicalBacking` remains non-authoritative, owner-native operation granularity remains intact, and deep-forcing the same incomplete declaration as enabled fails.
- [x] 2.5 Reuse the existing ZFS pool dataset map for host/aspect-local State + Realization contributions and emit plan-only `household/home` and `household/persist`; verify explicit dataset-to-State association, actual `rpool/safe/home` and `rpool/safe/persist` bindings, separate `/home` versus logical host-role `/persist` semantics, non-recursive coverage, unresolved production Targets/Integrations, and application-consistency limitations without namespace lookup, runtime probes, or secret reads.
- [x] 2.6 Add pure fixtures for native host, two-host migration, container/Incus host-backed access, Kubernetes hostPath/local PV/CSI PVC, microVM host share/native volume, PostgreSQL application access with non-authoritative physical backing, and reusable production/QA slots. Add a ZFS Realization providing `filesystem-read`, `zfs-snapshot`, and `zfs-send` whose Restic and native-ZFS Routes both resolve through capability matching while reporting only each configured owner workflow's actual consistency. Add a mutable-filesystem Restic fixture guaranteeing `live` that fails a stronger StateSlot requirement, plus a normal State that resolves through shared Integration+Target wiring with no Binding. Verify access projections do not become Realizations, backing cannot fulfill Routes, unsupported capabilities stay unsupported, and mount/dataset/exclusion gaps cannot claim whole-State coverage.
- [x] 2.7 Register owner projectors as typed internal implementation rather than public Integration/manifest data; project resolved fixtures into inactive `services.zrepl.settings` and `services.restic.backups` without generic owner-name switches. Verify projectors derive base source paths from Realization, reusable repository/credential/lifecycle inputs from shared Integration+Target wiring, and only irreducible State-specific narrowing/overrides from optional Binding. Verify logical Target identity is backend-independent, StateSlots/Routes do not duplicate native retained formats, a normal State needs no Binding, stable owner job identity and adapter entry points reach native configuration, and no real service or Preserve-managed lifecycle workflow is emitted. Verify multiple Routes to one Target are not reported as independent failure-domain copies.

## 3. Minimal coordinator and owner-oriented adapters

- [x] 3.1 Add shared manifest, target-qualified native point, adapter-version, operation, error, and restore-receipt types plus the minimal Rust `homelab-preserve` package; distinguish optional semantic payload representation from Integration-owned native retained-point representation, and verify round trips retain/display both plus unknown opaque producer provenance under namespaced keys without interpreting them or making them part of `stateId`. Preserve required evidence and reject unsupported required schema or adapter versions.
- [x] 3.2 Implement human/JSON `plan`, `status`, and `points <state>`; verify planning never starts an owner operation, configured-but-unobserved differs from an observed empty native catalog, and native point identity is mapped without inventing synchronized capture or protection health.
- [x] 3.3 Implement fixed `run <state> --route <route>` dispatch for one declared lifecycle-owner action with concurrent bounded output, timeout/cancellation, checked exit status, and structured errors; verify it cannot accept arbitrary owner-internal stages or execute an executable path from retained provenance.
- [x] 3.4 Implement the M1 scratch-only restore authorization, preflight/execution, and receipt-based `verify`; verify exact native point selection, traversal/existing destination/source or Target alias refusal, immediate pre-mutation revalidation, owner-default root/in-place restore refusal, semantic payload/native retained representation checks, and altered restored data failure without a force bypass. Verify scratch-only is runtime authorization rather than StateSlot semantics.
- [x] 3.5 Keep protocol version 1 limited to `describe`, `status`, `points`, `run`, `restore`, and `verify`, with namespaced owner payloads and optional capabilities; verify malformed/truncated/oversized responses, incompatible versions, unsupported operations, nonzero exits, and timeouts fail without hanging or moving bulk data through JSON.
- [x] 3.6 Package a separate test-only lifecycle-owner fixture adapter; verify manifest registration requires no coordinator source change or recompilation, one atomic fixture action stays atomic, and unsupported capture/protect/release/retry stages are not required by the common protocol.

## 4. Fixture-only direct-ZFS reference adapter

- [ ] 4.1 Add `homelab-preserve-zfs-reference` with an explicit fixture-only capability and strict configured source, target, and scratch ancestry checks; verify it is absent from all production host aspects and rejects out-of-root locators, hostile mount/share properties, and non-reference manifests.
- [ ] 4.2 Implement only synthetic single-dataset snapshot, retained local point, one full second-pool `zfs send`/`zfs receive`, and point enumeration using native snapshot names/GUIDs plus compact ZFS-native mapping properties; verify both routes identify the same reference capture, incomplete receive is not advertised, and no schedule, pruning, retention, incremental, resume, cursor, hold, journal, retry, or receiver lifecycle exists.
- [ ] 4.3 Implement exact-point restore into a new allowlisted scratch child and native-scope verification; verify the selected older point restores twice, source and target remain unchanged, unsafe destinations are refused, and post-restore divergence is detected without rollback, `receive -F`, or active-state replacement.

## 5. Independent conformance and CI

- [ ] 5.1 Expose the real inventory, strict synthetic plan, minimal coordinator, fixture adapter, ZFS reference adapter, and fast model/protocol checks through existing flake modules without editing generated `flake.nix`; verify package/check names evaluate on supported systems and `nix run .#write-flake` leaves generated output consistent.
- [ ] 5.2 Add a Linux two-disk NixOS VM test that invokes the shipped coordinator and reference adapter; verify synthetic regular files, historical versions, directories, symlink, hardlink, UID/GID, modes, representative ACL/xattr, sparse file, two native points, explicit older-point restore, and a second rehearsal with an independent oracle.
- [ ] 5.3 Exercise receiver failure, unsafe destinations, nested-dataset scope, altered-scratch verification failure, and source/coordinator-cache loss; verify truthful failure, no incomplete advertised point, preserved complete local point, source-independent receiver discovery/restore, and explicit filesystem-only consistency and fixture-only lifecycle limits.
- [ ] 5.4 Give fast preserve checks and the ZFS VM separate CI entries or groups with `fail-fast: false` and direct dispatch; verify no dependency on Incus/Kubernetes acceptance, moving images, credentials, homelab access, production lifecycle owners, or new self-hosted runners.

## 6. Documentation, verification, and handoff

- [ ] 6.1 Document exact model and operation entry points, protocol version 1, lifecycle-owner integration rules, target-native recovery evidence, safe scratch requirements, reference-adapter limits, and a small external adapter example; verify adding a zrepl, Restic/resticprofile, or borgmatic integration requires configuration plus a thin owner-level adapter rather than coordinator workflow changes.
- [ ] 6.2 Run formatting plus the narrow gen/model, Rust/protocol, package, generated-flake, and ZFS VM checks on supported environments; verify pre-existing Den/storage checks still pass, record exact commands/results, and leave any blocked check explicitly incomplete.
- [ ] 6.3 Complete the preserved packet handoff with actual jj changes, revisions, entry points, evidence, lifecycle-owner boundary, reference-adapter limitations, and safety statement; verify it confirms no production enablement, private-data access, schedule, retention, repository, disk, secret, network, router, or existing-active-change modification occurred and names the next separately authorized owner-selection step.
