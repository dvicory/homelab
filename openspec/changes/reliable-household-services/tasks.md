Open checkboxes are acceptance gates, not a claim that the existing media manifests are absent. Local implementation and disposable-runtime verification are authorized; production credentials, deployment and provider connection are not.

## 1. Media workloads and declared boundaries

- [x] 1.1 Render Radarr, Sonarr, Prowlarr, SABnzbd and Seerr with independent retained private configuration, native credentials, bounded workload settings and required internal connections. Exercise startup and authenticated API access against disposable pinned workloads; fail closed when a declared retained path or required credential is unavailable.
- [x] 1.2 Evaluate two instances of each Arr kind with independent state, key, root, category and semantic standard/optional-4K profile. Reject conflicting logical roots/categories without explicit sharing and missing/duplicate Seerr defaults; prove a secondary instance does not change the selected backend. Report shared GID-505 write access as coarse filesystem authority, not per-application path isolation.
- [ ] 1.3 Verify Seerr's household route uses native sign-in while Radarr, Sonarr, Prowlarr and SAB browser routes require administrator access, and internal automation still uses authenticated native APIs. Exercise a non-admin request-user path and administrator-route denial through the real edge acceptance owned by PR23; do not create another edge implementation here.

## 2. Managed configuration and integration

- [x] 2.1 Pin Configarr 1.32 and the selected local TRaSH/template inputs with verifiable upstream revision and content provenance. Check that CI rejects revision/content mismatch and that selected policy reconstructs without live upstream policy access.
- [x] 2.2 Use native Configarr as the only writer of declared Arr root records, semantic quality profiles, scored formats, SAB download-client settings and managed Prowlarr registrations. Prove that UI-owned Prowlarr records and media files survive removal of an undeclared Arr root record, credentials rotate safely, and reconciliation alone initiates no search or upgrade. Make malformed policy and rejected API writes fail the actual Job.
- [x] 2.3 Put media configuration after healthy workload Applications and PR24 Jellyfin/Jellarr readiness using Argo waves and one-shot PostSync hooks. Run staggered non-suspended six-hour Configarr and Seerr CronJobs with bounded deadlines and shared Job specs; local pinned-API runs prove idempotence. Prove the *hosted fresh cluster* converges from one Git revision without manual Argo resync.
- [x] 2.4 Consume the Jellyfin-owned endpoint, administrator credential and configured Movies library without generating a competing Jellyfin account or library. Prove pinned Seerr first-owner setup privately, subsequent stable-key reconciliation, synchronized library and selected Arr defaults; publish the requests route only in the operator-confirmed `ready` phase.

## 3. Pinned-runtime acceptance and handoff

- [x] 3.1 Run the bounded disposable integration against pinned Jellyfin, Radarr, Sonarr, Prowlarr, SABnzbd, Seerr and Configarr releases. Verify native credentials, Arr roots/profiles/scored formats/download clients, Prowlarr registrations, SAB configuration and Seerr onboarding; repeat reconciliation and confirm idempotence, unrelated-record/file preservation, credential rotation, malformed-input/API-error failure and no search/upgrade commands. Live providers/indexers are not required.
- [x] 3.2 Prove pinned Configarr creates authoritative declared root records and can remove undeclared records without deleting filesystem media. Delete the redundant custom Arr-root and Prowlarr-registration writers and overlapping tests; retain one reconciliation owner per field.
- [x] 3.3 Regenerate media operations guidance and manifests for actual routes, shared GID write authority, policy provenance, private first-owner and six-hour repair, integration prerequisites and operator-owned ciphertext/provider gates. Check source-versus-generated agreement without claiming PR22–24 whole-cluster recovery acceptance.
