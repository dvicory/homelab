Open checkboxes are acceptance gates, not a claim that the existing media manifests are absent. Local implementation and disposable-runtime verification are authorized; production credentials, deployment and provider connection are not.

## 1. Media workloads and declared boundaries

- [ ] 1.1 Render Radarr, Sonarr, Prowlarr, SABnzbd and Seerr with independent retained private configuration, native credentials, bounded workload settings and required internal connections. Exercise startup and authenticated API access against disposable pinned workloads; fail closed when a declared retained path or required credential is unavailable.
- [ ] 1.2 Evaluate two instances of each Arr kind with independent state, key, root, category and profile. Reject conflicting logical roots/categories without explicit sharing and missing/duplicate Seerr defaults; prove an added secondary instance does not change the selected Seerr backend. Report shared GID-505 media write access as coarse filesystem authority, not enforced per-application path isolation.
- [ ] 1.3 Verify Seerr's household route uses native sign-in while Radarr, Sonarr, Prowlarr and SAB browser routes require administrator access, and internal automation still uses authenticated native APIs. Exercise a non-admin request-user path and administrator-route denial through the real edge acceptance owned by PR23; do not create another edge implementation here.

## 2. Managed configuration and integration

- [ ] 2.1 Pin Configarr and selected TRaSH/template paths with verifiable upstream revision/content provenance. Check that CI rejects a revision/content mismatch and that selected policy can reconstruct without live upstream policy access; preserve declarative local overrides.
- [ ] 2.2 Reconcile only declared Arr roots, quality/profile and download-client fields, Prowlarr registrations and SAB settings, with one owner per field. Exercise managed-field repair and preservation of unmanaged records and existing media; verify policy reconciliation alone initiates no searches, upgrades or deletion.
- [ ] 2.3 Put media configuration after healthy workload Applications and PR24 Jellyfin/Jellarr integration readiness using Argo Application waves and one-shot PostSync hooks; retain bounded API readiness checks and ordinary re-sync behavior. Prove a fresh disposable cluster converges from one Git revision without manual Argo resync, and that no suspended CronJobs imply an unsupported operator workflow.
- [ ] 2.4 Consume Jellyfin-owned endpoint, integration credential and configured library identities without generating a competing Jellyfin owner or library setup. Prove Seerr recognizes existing library availability and connects to the explicitly selected Radarr/Sonarr instances through supported pinned-release APIs; document only an actually unavoidable operator gate.

## 3. Pinned-runtime acceptance and handoff

- [ ] 3.1 Run a bounded disposable integration against the actual pinned Jellyfin integration, Radarr, Sonarr, Prowlarr, SABnzbd, Seerr and Configarr releases. Verify declared API credentials, Arr roots, Configarr profile/quality/download-client payloads, Prowlarr registrations, SAB configuration and Seerr onboarding; repeat reconciliation and confirm idempotence and unmanaged/UI-owned record preservation. Live Usenet providers/indexers are not required.
- [ ] 3.2 In that real API run, test Configarr root-folder management. If it reliably establishes the declared roots, remove the custom root writer and its overlapping tests; otherwise retain only the thin missing-field writer with evidence of the pinned limitation. Verify no two reconcilers own the same field.
- [ ] 3.3 Update media-family operations guidance and generated references to describe actual routes, shared GID write authority, selected policy provenance, integration prerequisites and remaining production/provider gates. Check source-versus-generated agreement without broadening the PR22–24 or general recovery acceptance scope.
