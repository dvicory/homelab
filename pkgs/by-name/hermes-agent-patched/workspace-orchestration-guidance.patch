diff --git a/tests/plugins/test_workspace_handoff.py b/tests/plugins/test_workspace_handoff.py
index 1673aa2..4c2da66 100644
--- a/tests/plugins/test_workspace_handoff.py
+++ b/tests/plugins/test_workspace_handoff.py
@@ -620,3 +620,13 @@ def test_failed_publication_replay_does_not_skip_crash_detection(
         assert operation["state"] == "pending"
         assert "revision.invalid_state" in operation["last_error"]
 
+
+def test_create_schema_explains_trusted_sequential_handoff():
+    description = kanban_tools.KANBAN_CREATE_SCHEMA["description"]
+
+    assert "do not pre-create the receiver outside the producer task" in description
+    assert "inherit_parent_workspace_output=true" in description
+    assert "complete with workspace_outputs" in description
+    complete = kanban_tools.KANBAN_COMPLETE_SCHEMA["parameters"]["properties"]
+    assert "transfers file bytes between tasks" in complete["workspace_outputs"]["description"]
+    assert "do not transfer bytes to child tasks" in complete["artifacts"]["description"]
diff --git a/tools/kanban_tools.py b/tools/kanban_tools.py
index b31bf05..47ce5bb 100644
--- a/tools/kanban_tools.py
+++ b/tools/kanban_tools.py
@@ -1358,8 +1358,9 @@ KANBAN_COMPLETE_SCHEMA = {
                 "maxItems": 128,
                 "description": (
                     "Optional relative paths beneath /workspace to publish as one immutable "
-                    "revision before this task becomes done. Omit for ordinary completion. "
-                    "Host paths, destination remapping, and storage identifiers are not accepted."
+                    "revision for a trusted direct child. This transfers file bytes between tasks; "
+                    "artifacts does not. Omit when no downstream worker needs the bytes. Host paths, "
+                    "destination remapping, and storage identifiers are not accepted."
                 ),
             },
             "artifacts": {
@@ -1378,7 +1379,9 @@ KANBAN_COMPLETE_SCHEMA = {
                     "intermediate scratch files and references that "
                     "are not the deliverable. The path must exist "
                     "on disk when the notifier runs; missing files "
-                    "are silently skipped."
+                    "are silently skipped. Artifacts are human-facing "
+                    "attachments and do not transfer bytes to child tasks; "
+                    "use workspace_outputs for a child handoff."
                 ),
             },
             "board": _board_schema_prop(),
@@ -1495,7 +1498,11 @@ KANBAN_CREATE_SCHEMA = {
         "orchestrator workers to fan out — decompose work into child "
         "tasks with specific assignees, link them into a pipeline, "
         "then complete your own task. The dispatcher picks up the new "
-        "tasks on its next tick and spawns the assigned workers."
+        "tasks on its next tick and spawns the assigned workers. For a sequential "
+        "file handoff, do not pre-create the receiver outside the producer task. "
+        "Instead, tell the producer to create its one direct child with "
+        "inherit_parent_workspace_output=true, then complete with workspace_outputs; "
+        "trusted producer identity is required for that import."
     ),
     "parameters": {
         "type": "object",
